#include <stdio.h>

int main()
{
    // Area of Square
    float a;
    printf("Enter Length:");
    scanf("%f", &a);

    float square = a * a;
    printf("Area of Square %f", square);

    return 0;
}
